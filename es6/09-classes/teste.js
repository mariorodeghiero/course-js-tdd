const dog = "black";
const cat = ["tofu", 11, true, false, hello(), undefined];

let yes = {
  nome = "mario",
  age = 30,
  cats = true,
  dogs = false,
  hello() {
    return 2 + 2;
  }
}

let time = [
  {
    nome = "bento",
    animal = true
  }
]

console.log('------------------------------------');
console.log("Ola esse é um teste");
console.log(`Teste para testar interpolar ${this.time.nome}`)
console.log(`Teste para testar interpolar ${cat[4]}`)
console.log('------------------------------------');

function Animal () {
  name = name + name;
  if (name === name) {
    name = 3 * 5;
  } else {
    return null;
  }
}

for (let index = 0; index < array.length; index++) {
  const element = array[index];

}

array.forEach(element => {

});

while (true) {
  switch (false) {
    case mario:

      break;
    case tofu:

      break;
    default:
      break;
  }
}

// Este é um comentario para testar


/*
*
* Este é um comentario para testar
* o tema creado.
*
*/

